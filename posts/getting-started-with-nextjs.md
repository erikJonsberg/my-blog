---
slug: "getting-started-with-nextjs"
title: "Getting Started with Nextjs"
description: "Learn how to build a blog using Nextjs and Tailwindcss"
image: "getting-started.jpg"
excerpt: "Building a blog using Nextjs and Tailwindcss is easy"
date: "2023-06-08"
authorName: "Erik Jonsberg"
authorAvatar: "profile.jpg"
imageAlt: "Nextjs and Tailwindcss"
isFeatured: false
---

## That is when you can experience true joy

When you have no fear. For the lack of a better word I call them hangy downs. This is the way you take out your flustrations.

## These trees are so much fun

### That's your challenge for the day

- I get started on them and I have a hard time stopping.
- And maybe a little bush lives there.
- A thin paint will stick to a thick paint.
- How do you make a round circle with a square knife?

[That's your challenge for the day](https://google.com). Think about a cloud. Just float around and be there. We'll put a happy little sky in here.

![My Alt {{w: 1200, h: 630}}](https://images.unsplash.com/photo-1565799284935-da1e43e7944e?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w0MTQzMzh8MHwxfHNlYXJjaHwzfHxib2J8ZW58MHwwfHx8MTY4Njg1MTAzNnww&ixlib=rb-4.0.3&q=80&w=1080)

## Now we'll take the almighty fan brush

1. Here we're limited by the time we have.
2. Use your imagination, let it go.
3. Just think about these things in your mind - then bring them into your world.

## When you do it your way you can go anywhere you choose

It's almost like something out of a fairytale book.

```js
const syntaxTheme = oneDark;

  const MarkdownComponents: object = {
    code({ node, inline, className, ...props }) {
      const hasLang = /language-(\w+)/.exec(className || '');
      const hasMeta = node?.data?.meta;

      const applyHighlights: object = (applyHighlights: number) => {
        if (hasMeta) {
          const RE = /{([\d,-]+)}/;
          const metadata = node.data.meta?.replace(/\s/g, '');
          const strlineNumbers = RE?.test(metadata)
            ? RE?.exec(metadata)[1]
            : '0';
          const highlightLines = rangeParser(strlineNumbers);
          const highlight = highlightLines;
          const data: string = highlight.includes(applyHighlights)
            ? 'highlight'
            : null;
          return { data };
        } else {
          return {};
        }
      };
```

**It looks so good,** I might as well not stop. `Be careful`. You can always add more - *but you can't take it away*. But we're not there yet, so we don't need to worry about it. If we're going to have animals around we all `have to be concerned` about them and take care of them. **Isn't that fantastic?** Use what happens naturally, don't fight it.

> That's why I paint - because I can create the kind of world I want - and I can make this world as happy as I want it. We'll throw some old gray clouds in here just sneaking around and having fun. Let the paint work.
>